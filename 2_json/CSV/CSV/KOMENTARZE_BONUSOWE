            /*
            foreach (var line in fileContent)
            {
                Console.WriteLine(line);
            }
            
            foreach (var line in File.ReadLines(path))
            {
                Console.WriteLine(line);
            }
            
            // Analogicznie co ""
            var wrt = string.Empty;
            
            // Sprawdzenie czy string dostarczony jako argument jest pusty lub null
            if(string.IsNullOrEmpty(wrt)){ }

            // Sprawdzenie czy string dostarczony jako argument jest białym znakiem lub nullem
            if(string.IsNullOrWhiteSpace(wrt)){ }
            
            */

            //      studentsSet.Add(student); //w pętli
            /*  if (studentsSet.Contains())
            {
                Console.WriteLine("Student już jest dodany");
            }
            */
            /*
            if (!studentsSet.Add(x)) // jak jest błąd 
            {
                Console.WriteLine("Student już jest dodany");
                using var sw = new StreamWriter("log.txt");
                sw.WriteLine($"Student {x.Imie} {x.Nazwisko} znajduje się już w bazie.");
            }
*/